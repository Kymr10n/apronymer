{
  "version": "0.2.0",
  "configurations": [
    
    {
      "name": "Debug Backend + Run Frontend",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/apronymer.exe",
      "args": [],
      "cwd": "${workspaceFolder}/backend",
      "preLaunchTask": "Build Backend",
      "terminal": "integrated"
    }
  ]
}